export interface CollapseProps {
    title: React.ReactNode // Título do colapso, exibido como o cabeçalho ou o título do componente
    children: React.ReactNode; // Conteúdo que será exibido dentro do componente colapsável. Pode ser qualquer elemento React.
}